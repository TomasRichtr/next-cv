import {
  LOCALES,
} from "@/constants/locales";

const i18nConfig = {
  locales: LOCALES,
  defaultLocale: LOCALES[0],
  prefixDefault: false,
  localeDetection: false,
};

export default i18nConfig;
