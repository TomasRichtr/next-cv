export enum Sizes {
    XS = "xs",
    SM = "sm",
    MD = "md",
    LG = "lg",
    XL = "xl",
}

export enum Colors {
    Primary = "primary",
    Secondary = "secondary",
    Accent = "accent",
    Info = "info",
    Success = "success",
    Warning = "warning",
    Error = "error",
    Neutral = "neutral",
}

export enum Styles {
    Soft = "soft",
    Text = "text",
    Outline = "outline",
    Gradient = "gradient",
}
